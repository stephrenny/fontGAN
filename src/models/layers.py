import torch
from torch import nn
from tqdm.auto import tqdm
from torchvision import transforms
import numpy as np


class ResBlock(nn.module):
    def __init__()


class GeneratorBlock(nn.module):
    pass


"""
Tells the discriminator what kind of font it should be looking for
Is conditioned on the same condition that the generator is conditioned on, minus the noise vector
"""


class DiscriminatorPrimer(nn.module):
    pass


class DiscriminatorBlock(nn.module):
    pass
